# Shared Libraries

Shared libraries are organized by runtime ecosystem.

- `go/` — Go shared libraries.
- `python/` — Python AI/shared libraries.
- `node/` — Node/TypeScript shared packages.

Library implementation requires authorized implementation-unit scope.
