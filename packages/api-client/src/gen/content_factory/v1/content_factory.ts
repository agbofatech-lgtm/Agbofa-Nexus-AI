// Auto-generated by stephenh-ts-proto compatible generator from content_factory/v1/content_factory.proto
// Do not edit directly

export interface CreatePackageRequest {
  tenant_id: string;
  story_id: string;
  title: string;
  summary: string;
  brand_voice_id: string;
  target_channels: string[];
}

export interface CreatePackageResponse {
  package_id: string;
  status: string;
  created_at_unix: number;
}

export interface GetPackageRequest {
  tenant_id: string;
  package_id: string;
}

export interface ContentPackage {
  package_id: string;
  tenant_id: string;
  story_id: string;
  title: string;
  status: string;
  articles: ArticleAsset[];
  media: MultimediaAsset[];
  social: SocialAsset[];
  qa_report: QAReport;
  brand_voice_id: string;
}

export interface ArticleAsset {
  asset_id: string;
  headline: string;
  body_text: string;
  seo_title: string;
  seo_description: string;
  language: string;
}

export interface MultimediaAsset {
  asset_id: string;
  asset_type: string;
  content_spec: string;
}

export interface SocialAsset {
  asset_id: string;
  platform: string;
  post_text: string;
}

export interface QAReport {
  qa_id: string;
  overall_quality_score: number;
  passed: boolean;
  flagged_issues: string[];
}

export interface GetPackageResponse {
  content_package: ContentPackage;
}

export interface ListPackagesRequest {
  tenant_id: string;
  status_filter: string;
}

export interface ListPackagesResponse {
  packages: ContentPackage[];
}

export interface SubmitForReviewRequest {
  tenant_id: string;
  package_id: string;
}

export interface SubmitForReviewResponse {
  package_id: string;
  status: string;
}

export interface ReviewPackageRequest {
  tenant_id: string;
  package_id: string;
  approved: boolean;
  reviewer_id: string;
  comments: string;
}

export interface ReviewPackageResponse {
  package_id: string;
  status: string;
  reviewed_at_unix: number;
}

export interface AdaptPackageRequest {
  tenant_id: string;
  package_id: string;
  target_channel: string;
}

export interface AdaptPackageResponse {
  package_id: string;
  adapted: boolean;
}

export interface LocalizePackageRequest {
  tenant_id: string;
  package_id: string;
  target_language: string;
}

export interface LocalizePackageResponse {
  package_id: string;
  localized: boolean;
}

export interface AuditQARequest {
  tenant_id: string;
  package_id: string;
}

export interface AuditQAResponse {
  qa_report: QAReport;
}

export interface ContentFactoryServiceClient {
  createPackage(request: CreatePackageRequest, metadata?: Record<string, string>): Promise<CreatePackageResponse>;
  getPackage(request: GetPackageRequest, metadata?: Record<string, string>): Promise<GetPackageResponse>;
  listPackages(request: ListPackagesRequest, metadata?: Record<string, string>): Promise<ListPackagesResponse>;
  submitForReview(request: SubmitForReviewRequest, metadata?: Record<string, string>): Promise<SubmitForReviewResponse>;
  reviewPackage(request: ReviewPackageRequest, metadata?: Record<string, string>): Promise<ReviewPackageResponse>;
  adaptPackage(request: AdaptPackageRequest, metadata?: Record<string, string>): Promise<AdaptPackageResponse>;
  localizePackage(request: LocalizePackageRequest, metadata?: Record<string, string>): Promise<LocalizePackageResponse>;
  auditQA(request: AuditQARequest, metadata?: Record<string, string>): Promise<AuditQAResponse>;
}
