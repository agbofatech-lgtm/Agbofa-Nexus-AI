import { BusinessHeader } from "@/components/features/business/BusinessHeader";
export function DistributionHeader() {
  return (
    <BusinessHeader
      eyebrow="16-channel command center"
      subtitle="Coordinate brand and manual founder distribution without claiming unverified social connections or analytics."
      title="Distribution"
    />
  );
}
